document.cookie = "username=Intellectro Norman";
let x = document.cookie;
// alert(x)

//! COOKIES PARAMETER

//* username which accomulates the user's name
//* expires which tells when the cookie will expire.
//* path => shows the file path the cookie belongs to.
